var Playlist = (function(){
  
  return {
    // set a new playlist with the next/prev pointers 
    // send it a list of ids in order to create the playlist from, and perhaps, a name.
    set: function(idList, name) {
    },

    previous: function() {
    },

    next: function() {
    },

    // get back an old playlist
    revert: function() {
    }
  };
})();
